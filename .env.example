# ==============================================
# Blooming Beginnings - Environment Variables
# ==============================================
# Copy this file to .env.local and fill in your values

# ==============================================
# Database (Vercel Postgres / Neon)
# ==============================================
# Get these from your Vercel Postgres dashboard
POSTGRES_URL="postgres://user:password@host/database"
POSTGRES_PRISMA_URL="postgres://user:password@host/database?pgbouncer=true"
POSTGRES_URL_NO_SSL="postgres://user:password@host/database"
POSTGRES_URL_NON_POOLING="postgres://user:password@host/database"
POSTGRES_USER="user"
POSTGRES_HOST="host"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="database"

# ==============================================
# World ID / MiniKit Configuration
# ==============================================
# Get your App ID from https://developer.worldcoin.org
WORLD_APP_ID="app_your_app_id_here"

# Action IDs (create these in the Developer Portal)
WORLD_VERIFY_ACTION="verify-human"
WORLD_DAILY_BONUS_ACTION="claim-daily-bonus"

# ==============================================
# Token Distribution (World Chain)
# ==============================================
# Private key for the distributor wallet (KEEP THIS SECRET!)
# This wallet needs ETH for gas and tokens to distribute
DISTRIBUTOR_PRIVATE_KEY="0x..."

# ERC20 token contract address for rewards
REWARD_TOKEN_ADDRESS="0x..."

# Daily bonus amount in wei (200 tokens with 18 decimals = 200000000000000000000)
DAILY_BONUS_AMOUNT="200000000000000000000"

# Game reward multiplier (score * multiplier = reward in wei)
GAME_REWARD_MULTIPLIER="1000000000000000"

# ==============================================
# Session & Security
# ==============================================
# JWT secret for signing session tokens (generate a strong random string)
# You can generate one with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-at-least-32-characters"

# ==============================================
# Application URLs
# ==============================================
# Your app's public URL (used for CORS and callbacks)
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# Use testnet instead of mainnet (for development)
NEXT_PUBLIC_USE_TESTNET="false"

# ==============================================
# Optional: Custom RPC (if not using public endpoints)
# ==============================================
# WORLD_CHAIN_RPC_URL="https://your-custom-rpc.com"
